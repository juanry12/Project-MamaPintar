import 'package:flutter/material.dart';

class SpeciaList{
  String iconUrl,title;
  Color backgroundColor;
  String bannerTitle,bannerDescription;
  int price;

  SpeciaList({this.iconUrl, this.title, this.backgroundColor, this.bannerTitle,
      this.bannerDescription, this.price});
}